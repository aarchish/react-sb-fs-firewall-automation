package com.csx.firewall_automation.repository;

import com.csx.firewall_automation.model.TF_IPs;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TF_IPsRepository extends JpaRepository<TF_IPs, Long> {
}
