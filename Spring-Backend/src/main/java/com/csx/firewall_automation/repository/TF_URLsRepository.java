package com.csx.firewall_automation.repository;

import com.csx.firewall_automation.model.TF_URLs;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TF_URLsRepository extends JpaRepository<TF_URLs, Long> {
}
